// Copy this file to `secrets.rs` and fill in your own keys.
// `secrets.rs` is gitignored so real keys never get committed.

// SteamGridDB API key — used for auto-fetching cover art.
// Get a free key at https://www.steamgriddb.com/profile/preferences/api
pub const SGDB_API_KEY: &str = "your-steamgriddb-api-key-here";
